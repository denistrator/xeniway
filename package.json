{
  "name": "job-tracker",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k \"bun --cwd apps/api dev\" \"bun --cwd apps/web dev\"",
    "test": "bun run --cwd packages/shared test && bun run --cwd apps/api test && bun run --cwd apps/web test",
    "test:e2e": "playwright test",
    "typecheck": "bun run --cwd packages/shared typecheck && bun run --cwd apps/api typecheck && bun run --cwd apps/web typecheck",
    "build": "bun run --cwd apps/web build && bun run --cwd apps/api typecheck",
    "db:up": "docker compose -f infra/docker-compose.yml up -d postgres",
    "db:down": "docker compose -f infra/docker-compose.yml down",
    "redis:up": "docker compose -f infra/docker-compose.yml up -d redis",
    "redis:down": "docker compose -f infra/docker-compose.yml stop redis",
    "redis:check": "docker compose -f infra/docker-compose.yml exec -T redis redis-cli ping",
    "db:migrate": "bun run --cwd apps/api db:migrate",
    "db:seed": "bun run --cwd apps/api db:seed",
    "format": "biome format --write .",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@biomejs/biome": "2.5.11",
    "@playwright/test": "latest",
    "@types/node": "latest",
    "concurrently": "latest",
    "typescript": "latest"
  }
}
