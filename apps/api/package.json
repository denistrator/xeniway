{
  "name": "@xeniway/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:migrate": "bun --env-file=../../.env src/db/migrate.ts",
    "db:seed": "bun --env-file=../../.env src/db/seed.ts"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.2",
    "@xeniway/shared": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "elysia": "^1.4.30",
    "nodemailer": "^10.0.8",
    "postgres": "^3.4.9",
    "redis": "^6.2.1",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "bun-types": "^1.4.2",
    "@types/nodemailer": "^8.0.1",
    "vitest": "^5.0.0",
    "typescript": "^7.0.2"
  }
}
